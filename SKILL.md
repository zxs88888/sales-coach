---
name: sales-coach
description: "Sales Coach 销售教练作战卡生成器。当用户想要分析某个客户或公司（输入客户名称与我方公司身份）并获取拜访建议、销售策略、客户情报、作战卡时使用本技能。也用于反哺客户线索（在对话中随时补充客户新情报后重新分析）。触发语示例：分析XX客户、帮我准备拜访YY公司、给ZZ的销售策略或作战卡、客户情报、销售教练、sales coach、推进到POC阶段、补充他们CTO来自阿里云。本技能用 WebSearch/WebFetch 联网采集情报、用文件持久化客户档案、支持跨会话累积与重算。"
---

# Sales Coach — 销售教练作战卡

## Overview

把"分析某客户 + 我方立场 → 联网侦察 → 生成可落地作战卡 → 存档 → 随时反哺重算"做成一个可复用的
Agent 工作流。替代原小程序方案中"付费搜索 API + 云函数 + 云数据库"整套架构：

- `WebSearch` / `WebFetch` 替代付费搜索 API（免费、自动处理限流/反爬）
- Agent 自身即 LLM，替代原 DeepSeek 调用
- 文件系统（markdown）替代云开发 DB，承载客户档案与策略快照
- 对话上下文天然支撑「反哺线索 → 重新分析」闭环

数据目录（默认，可由用户改）：`~/.workbuddy/sales-coach/`
- `clients/<归一化名>.md` —— 每个客户一份档案（情报缓存 + 已验证/推断 + 决策链 + 策略快照）
- `myCompany.json` —— 我方立场开关（`active` 指向矩阵预设键或 `profiles` 自定义键）

## CRITICAL RULES（硬性铁律，违反任一条都会产出误导性结果）

1. **入口先查档案（跨会话反哺的总开关）**：任何客户相关请求，第一步必须
   `Read ~/.workbuddy/sales-coach/clients/<归一化名>.md`。存在 → 进「反哺/追加模式」，**绝不从头全量重搜**
   （除非用户明确要求"刷新情报"）；不存在 → 进「新建模式」走完整 Phase 0→4。漏查旧档 = 反哺闭环失效。

2. **双角色隔离**：所有"建议/行动项"的主语必须是「我方/我们」，禁止以客户视角输出行动项。
   站在我方立场给建议，但分析客户时只做客观描述（主语为"客户"）。两者不可混。

3. **WebSearch 提取纪律**：WebSearch 返回的是**合成摘要块，非原始 URL 列表**。
   每条结果须判断它支撑哪个模块；能定位官网/新闻原文就记 URL，否则标「推断」。
   摘要里的公司名/人名/数字，**必须能回指到某条搜索结果的 URL**；回指不到的禁止写进作战卡正文
   （可放「推断」段并注明"无来源"）。vendor/供应商图谱只列有 URL 证据者，否则空数组。

4. **冲突优先级**（口述 vs 缓存冲突时）：用户已验证 > 用户口述 > 较新联网已验证 > 推断。
   最近口述覆盖陈旧联网情报（如"其实刚迁阿里云"覆盖缓存里的"AWS"）。

5. **文件名安全**：归一化后做文件名净化，**剔除 `/`、`\`、`..`、控制字符与首尾点号**，
   空格折叠为 `_`；保留中文/字母/数字/`_-`。确保只落在 `clients/` 目录内，防止路径遍历覆盖别处文件。
   （切勿仅保留 `[a-z0-9_-]` —— 会抹掉中文客户名如「蔚来」。）

6. **全量重合成 + 变更摘要**（反哺不做外科手术式 patch）：每次反哺都基于合并后的完整档案重新生成整张卡，
   再输出独立「变更摘要」（新增/移除/调整 三类 bullet），不靠精准改某模块（易改漏）。

7. **原子写**：写档案用「临时文件 → 重命名」（`mv tmp final`），写前留备份，防止写一半损坏 md。

8. **全部 WebSearch 失败不硬死**：若 0 条有效结果 → 自动转「探测清单 + 提示口述」，与紧急模式等价，
   不报错退出。

## Workflow

### Phase 0 — 立场绑定（Stance）

- 解析 `--company X` 覆盖参数；否则读 `myCompany.json.active` 立场：
  - 若 active 命中 `companyMatrix.json` 预设键 → 用该键档案（预设档案单一真源在矩阵，不在 myCompany 重复存）；
  - 若 active 命中 `myCompany.json.profiles` 自定义键 → 用该档案；
  - 若 active 为空 → 内联追问（见下）。
- 若立场未设（首次），**内联追问一句话**即可继续（不要卡死整个流程）：
  「请一句话说明我方公司：核心产品 / 最大优势 / 主要竞对 / 想避开的话术」。
- 命中 `references/companyMatrix.json` 的 9 家（tencent/aliyun/huawei/aws/azure/moonshot/zhipu/baichuan/qianwen-office）
  直接用；未命中则把用户描述存为 `myCompany.json.profiles[自定义 key]`，并将 `active` 设为该 key（字段见 §Data schema）。
- 用户修正某预设公司的竞对弱点时，存 `profiles[active]` 覆盖档案，运行时 **merge 覆盖**矩阵项。

### Phase 1 — 采集情报（Intel Collection）

**1a. 归一化 + 查档**（见 CRITICAL #1、#5）
- 归一化：小写 → 去「有限公司/股份有限公司/有限责任公司/集团/科技/技术/股份/公司/中国」等后缀
  → 英文别名映射（如 NIO→nio、蔚来→蔚来）→ 文件名净化（CRITICAL #5）。
- `Read` 旧档；存在则进反哺模式（Phase 5），以下采集步骤按"仅补缺失/用户要求刷新"执行。

**1b. 消歧检查点**（新建 + 短名/易混淆名时）
- 先 1 路探测（如 `{客户全名} 官网`）。若命中多个主体（"字节"→字节跳动 vs 某字节科技），
  **向用户确认**：「你说的是 <全称/官网> 吗？」再建卡。误建主体 = 给错公司出卡。

**1c. 搜索（~8–10 路）**
- 按 `references/searchQueries.md` 的**真实查询串模板**跑 WebSearch（分组：画像/战略/技术招聘/协同办公工具采用/
  AI Agent供应商/风险决策/预算项目）；海外主体（英文名或被标注 locale=en）切英文模板（见 searchQueries.md 末尾）。
- **去重 + 跳过已答类别**：同分析内不重复相同 query；用户口述已答的类别（"他们用 AWS"）直接跳过该类搜索。
- **单路失败不阻断**；高价值结果（官网、36kr/huxiu 原文）用 `WebFetch` 补细节。
- **来源质量偏好**：一手源（官网/财报/新闻稿）优先，聚合/SEO 内容降置信，不当事实。

**1d. 紧急模式（跳过搜索）**
- 用户说"跳过搜索 / 紧急模式 / 马上要见" → **完全不调 WebSearch**，仅凭口述情报 + 立场生成作战卡，
  数据质量声明中醒目标「⚠️ 未联网核实，以下基于你口述信息」。

**1e. 缓存进档**
- 把每路有效摘要按类别存入档案 `## 情报缓存`，**每条带日期戳**；反哺时直接复用，不重复烧搜索额度。

### Phase 2 — 丰富度自评（Richness Self-Assessment）

- 按覆盖类别数 + 摘要字符数 → 1/2/3 星；缓存条目 >60 天 → 降一档置信 + 提示"建议刷新情报"。
- ≤1 星 → 转「探测清单」模式（见 battleCardTemplate.md 的探测清单结构），减少臆测、改给核实清单。

### Phase 3 — 生成作战卡（Battle Card）

- 按 `references/battleCardTemplate.md` 输出 Markdown 作战卡，含：
  **顶部 30 秒速读 TL;DR** + 6 模块（①客户速写 ②战略动态 ③技术架构推断 ④协同/办公+AI工具图谱
  ⑤痛点推演 ⑥分阶段销售策略）+ **买家角色分支**（CTO/采购/业务负责人/老板自适应必聊3问与话术）
  + **常见异议应对**（每客户 2–3 条最可能异议 + 我方基于立场的应对）+ 探测清单结构 + 数据质量声明。
- 硬性规则（CRITICAL #2、#3）：站在我方立场、从我方产品矩阵出发；每条事实标**来源标注 [代号:说明]**或「推断」；
  禁止贬低友商（遵守 `avoidPhrases`）；竞对差异化基于我方优势。
- **生成自检清单（CRITICAL #8，落盘前必过）**：生成后逐项核对，任一项缺失则补全再进 Phase 4：
  1. 顶部 TL;DR 含**强制元数据**：一句话策略 + 必聊3问 + 当前阶段 + 丰富度星级 + 数据质量。
  2. **结构化 META 块（可视化头必填）**：`# 标题` 之后、`## TL;DR` 之前放 `<!-- META ... -->` 六字段——
     `stance / collected / ecosystem / metrics(≥3) / stage / roles / moves`。缺字段→对应可视化组件跳过（不报错），
     但**须与 md 正文同时产出**，否则仪表盘头为空。**通用约束**：所有可视化由 META / 模板结构驱动，不写死客户名。
  3. 模块⑥含全部必填子字段：当前阶段 / 阶段目标 / 必聊3问 / 可带材料 / 离开前必须确认 / **下一步日历提醒**（须从 `stagePlaybooks.md` 当前阶段推导具体动作+时间窗）。
  3. 每条事实带 **[代号:说明]** 来源标注或「推断」；**数据质量声明如实写"来源为代号非 URL"**，不得谎称"见链接"。
  4. 角色权重若打破"CTO ↓"通例（如技术驱动客户 CTO 最高），须注明理由。
  5. 行业排名/份额若不同口径有区间，已注明口径与区间。

### Phase 4 — 持久化（Persist）

- **前置 gate**：须先通过 Phase 3 生成自检清单（CRITICAL #8），任一项缺失则回 Phase 3 补全，不落盘。
- 写/更新 `clients/<归一化名>.md`：基础情报、已验证 vs 推断、决策链、当前阶段、情报缓存（带日期戳）、
  本次策略快照（追加历史）。**原子写**（CRITICAL #7）。
- **可视化 HTML 作战卡（标准产出，必做）**：md 落盘后运行
  `python3 references/gen_card_html.py clients/<归一化名>.md`
  → 同目录生成 `<归一化名>_card.html`（原子写，复用 md 全部内容与来源代号）。
  **随后用 `present_files` 打开该 HTML**：`.md` 在右侧预览面板只列卡片、不渲染正文，
  而 `.html` 会直接在面板渲染，确保用户能直观看到作战卡。每次跑客户都走这一步，无需用户额外要求。
  - **META 驱动仪表盘头**：指标瓷砖 / 阶段流水线 / 策略行动瓷砖 / 生态关系徽章 / 新鲜度徽章 / 下一步行动常驻条，
    均由 META 块渲染；模块②"近期动向"日期前缀 bullet → 竖向时间线；来源代号 → 置信度配色（含中性兜底）+ 图例；
    顶部粘性目录 + 纯 CSS 折叠 + 打印/PDF 优化对所有卡通用。
  - **md 是唯一真源**：HTML 每次由本脚本重新生成；**不要手工改 HTML**（下次跑会被覆盖）。行动条勾选态仅本地 UI、不回写 md。

### Phase 5 — 反哺重算（Feedback Recompute，核心闭环）

- 对话中随时喂线索（"他们 CTO 有阿里云背景" / "已推进到 POC"）：
  - `Read` 旧档 → 合并新线索（按 CRITICAL #4 定优先级）→ 标已验证/新推断。
  - **全量重合成**（CRITICAL #6）：基于合并后完整档案重新生成整张卡，再输出「变更摘要」高亮变化点。
  - 阶段推进：用户说"到 POC 阶段" → strategy 模块按 `references/stagePlaybooks.md` 新阶段剧本重算。
- **多客户消歧**：同对话聊过 ≥2 客户时，无客户名的指令默认作用于「最近客户」；若近轮涉及多个且歧义，
  主动问"指哪个？"，避免改错档案。

## Commands / 触发方式

- 关键词（最常用）：「分析 XX 客户」「拜访建议」「销售策略」「作战卡」「客户情报」「反哺：…」「推进到 X 阶段」。
- 显式：用户说「用 sales-coach 技能」或报客户名 + 要建议时即触发本技能。
- 管理指令：
  - 「列出我的客户档案」→ 读 `clients/` 目录列出（nice）。
  - 「删除客户档案 XX」→ 确认后移除对应 md（隐私 purge）。
  - 「刷新情报 XX」→ 忽略缓存、重跑全量搜索。

## Data schema（立场统一字段，skills 与模板一致引用）

```json
{ "name": "公司名", "products": ["产品/方案"], "strengths": ["优势"],
  "competitors": [{"name":"竞对","knownWeaknesses":["已知弱点"]}],
  "avoidPhrases": ["避免话术"], "defaultPositioning": "差异化切入点" }

> 预设公司档案以 `companyMatrix.json` 为单一真源；`myCompany.json.profiles` 仅存自定义公司或用户对预设的修正覆盖。
```

## Resources（references/ 按需加载）

- `companyMatrix.json` —— 9 家预设公司立场矩阵（tencent/aliyun/huawei/aws/azure/moonshot/zhipu/baichuan/qianwen-office）。
- `searchQueries.md` —— 8 组精简查询映射 + 真实中文 query 串模板 + 海外英文分支 + 限流/反爬提示。
- `battleCardTemplate.md` —— 作战卡 Markdown 模板（**含结构化 META 块规范** + TL;DR + 6 模块 + 角色分支 + 异议 + 探测清单 + 数据质量声明；模块②须用日期前缀 bullet、模块⑥"离开前确认/日历提醒"为解析字段）。
- `stagePlaybooks.md` —— 销售六阶段剧本，至少「线索培育/POC/谈判」三阶段写满实质话术，其余给骨架。
- `gen_card_html.py` —— Markdown 作战卡 → 可视化 HTML 编译器（标准库、零依赖）。Phase 4 自动调用，
  把 `clients/<归一化名>.md` 编译为同目录 `<归一化名>_card.html`。**通用可视化**：META 仪表盘头（指标瓷砖/阶段流水线/策略行动/生态徽章/新鲜度/下一步行动）、角色权重条形图、来源置信度配色+图例（中性兜底）、战略动态时间线、粘性目录+纯 CSS 折叠、风险/机会色块、打印/PDF 优化；解析 battleCardTemplate.md 的固定结构；无 META 或字段缺失→对应组件优雅跳过（存量卡兼容）。

## 交付分级（构建/验收用）

- **must**：入口查档、双角色隔离、提取纪律、反哺重算+变更摘要、冲突优先级、文件名安全、三阶段剧本、
  立场绑定、消歧检查点、情报缓存带日期戳、丰富度自评、紧急模式、全 WebSearch 失败降级、原子写、
  **Phase 4 自动生成可视化 HTML 作战卡并用 present_files 在右侧预览面板打开**；可视化头（指标瓷砖+阶段流水线+新鲜度+生态徽章+下一步行动）/ 角色权重条形图 / 来源置信度配色 / 战略动态时间线 / 粘性目录+折叠 / 打印导出。
- **nice（可后置）**：矩阵 merge 覆盖、海外分支、角色分支、异议库、删除/重置指令、档案总览。
